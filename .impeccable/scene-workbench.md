# Scene workbench

Mode: Operate. Scope: SceneBuilder only. Preserve Fake Case and Real Case in both the landing page and archive. The user meant the locally generated case named `demo`, not either preset case; that local entry was moved to a recoverable backup outside the active case listing.
The user pinned dottxt.ai's Products scroll style, bright colors, little text, and no promotional titles. Confirmed: manually review Codex output before submitting to the world model.

## Direction contract

THESIS: A continuous three-stage work surface for text/photos, intermediate image/prompt, and world generation.

OWN-WORLD: White, ice blue, and pale green sheets; thin square borders; existing Archivo/Noto Sans body typography; dark teal actions. No large headings.

STORY: Upload scene context and photographs, inspect Codex output, then explicitly generate and enter the world.

FIRST VIEWPORT: Small navigation above a full-width framed sheet; text at left, photo input at right, one action at bottom right. Mobile stacks inputs vertically.

FORM: User-pinned reference, code-led implementation. Large sheets overlap on desktop scroll; mobile stays in normal document flow. Actual task states drive scanning/loading, never timers pretending completion. Existing landing WebGL is reused and labeled as an existing scene during generation.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
