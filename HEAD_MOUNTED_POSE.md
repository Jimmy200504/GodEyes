# 從課堂 PnP 到頭戴 6DoF

> 最新決策：demo 改採螢幕旁固定單一 ArUco B，不需要 A／SLAM／IMU／NPU。已實作相機定位傳送器，見 [plan](pose/PLAN.md) 與 [操作指南](pose/README.md)。下文保留無標記 SLAM 的先前探索背景；不代表目前實作範圍。

## 已確認的硬體與既有成果

使用者確認：外接 Logitech webcam，沒有陀螺儀／IMU；已確認綁在頭上朝外拍環境，估計自身移動。朋友曾用相機拍攝人並偵測左右移動與旋轉；尚未取得程式碼，不能推定其已有完整 6DoF、公尺深度或 SLAM。

- 朋友原本的相機固定在外面拍人路線：可先沿用朋友的頭部／人體追蹤，轉成 API 契約；先確認輸出是 2D 座標、角度、還是有尺度的 3D pose。只有畫面左右與 yaw 時不能宣稱可測真實前後位移。不需要因為要操控 3D 就一定改用 SLAM。
- 本分支確定採相機綁在頭上朝外：仍採本分支原定的環境定位路線，朋友的拍人模型不能直接取代相機自定位。無 IMU 時先做純視覺 VO／SLAM，並用已知尺寸標記或距離補尺度；不能安排視覺慣性融合當作現有能力。
- 目前程式是相機 c2w pose 消費端；若輸入是 head pose，adapter 需明確定義頭部座標軸／旋轉方向，不能只把欄位改名就接上。

## 你學過的部分正好是基礎

PnP 問：「已知地圖上幾個 3D 點，也知道照片中各自的 2D 位置，相機在哪裡？」你的 ArUco 提供四個已知座標與尺寸，解出的 `w2c = [R,t]` 應反轉為 `c2w = [R^T,-R^T t]`。相機位置是 `-R^T t`，不是直接使用 `tvec`。見 [OpenCV PnP](https://docs.opencv.org/4.10.0/d5/d1f/calib3d_solvePnP.html)。

SLAM 多做的是「不知道場景點在哪裡時，自己建圖並反覆修正地圖和相機位置」。特徵式系統大致經過：跨影格找對應 → 有平移視差時三角化 → 對已建圖點估 pose → 局部最佳化 → 回到舊地方時辨識與校正累積誤差。PnP 是其中一種定位工具。VO 偏重連續相對運動；SLAM 加上維護地圖、重新定位和閉環校正。參考 [ORB-SLAM 論文](https://arxiv.org/abs/1502.00956) 和 [ORB-SLAM3](https://github.com/UZ-SLAMLab/ORB_SLAM3)。

SLAM 的輸出地圖可以只有稀疏特徵點；不必先重建可看的 NeRF 或 Gaussian 才能控制你的現有模型。追蹤的實體房間也不必長得像虛擬場景：先只映射相對運動即可。若要虛實物件對齊，則需另外做座標配準與尺度對齊。

使用者提供的 [Project 3](https://vaclisinc.github.io/2025_Computer_Vision/project_3/index.html) 與 [Project 4](https://vaclisinc.github.io/2025_Computer_Vision/project_4/index.html) 本次瀏覽工具無法讀取，以上課程對照依據使用者貼的程式碼，未宣稱已檢閱完整作業。

## 「最初轉一圈」的限制

像素經相機內參反投影會得到 ray 的方向，並不會自動得到 ray 上的深度。理想純旋轉只有方向變化，缺少三角化需要的基線。頭戴相機繞頸部轉動通常帶有一點平移，但是否足夠不可假設。初始化宜在有紋理、光線穩定的地方，同時做適量左右／前後位移，保留影格重疊。

單目影像本身只能還原到任意尺度：移動 1 個單位到底是多少公尺，需要已知尺寸標記、已知距離、雙目基線、深度，或完成校正且有足夠運動激勵的視覺慣性融合。IMU 不是插上就自動解決，需處理時間同步、相機-IMU 外參、偏置與初始化。純轉圈的退化與單目尺度問題是多視角幾何限制，實際系統能否初始化仍取決於演算法與運動。

你的單一 tag 版本可作為第一個「有尺度的真值對照」。必須固定 tag ID；不能每張任選 `corners[0]`，否則不同 tag 會被當成同一原點。多 tag 要先定義每個 tag 在同一世界座標的位置。看不到 tag 時這條 baseline 會失效，所以它不等於全房間 SLAM。平面 PnP 也可能有不穩定解，需重投影誤差、正深度與時間一致性檢查。相機內參／畸變必須對應實際解析度、裁切、鏡頭焦距；Viser 垂直 FOV 應使用 `fy` 與影像高度。

## CPU 與 NPU 分工

舊分支記錄 FRDM-i.MX93（約 2 GB RAM，當時可用 RAM／磁碟有限）；需重測現況。[NXP 官方入門文件](https://www.nxp.com/document/guide/getting-started-with-frdm-imx93%3AGS-FRDM-IMX93) 說明其 Ethos-U65 用於神經網路推論。這不是把任意 OpenCV/C++ 幾何運算自動變快的處理器。

| 工作 | 初版位置 | 後續評估 |
| --- | --- | --- |
| 相機擷取、曝光時間戳 | 邊緣 CPU / 驅動 | 先確認影格延遲、曝光與 rolling shutter |
| ORB、光流、匹配 | 邊緣 CPU | 先量測是否真的瓶頸 |
| 學習式特徵網路 | 尚未實作 | 可評估 NPU，需算子／量化／記憶體與精度測試 |
| PnP、RANSAC、三角化、BA、地圖管理 | 邊緣 CPU | 限制關鍵影格與地圖大小，實測時間 |
| 姿態接收、Three.js/Spark 渲染 | 外部電腦 | 已有模擬串接原型 |

[SuperPoint](https://github.com/magicleap/SuperPointPretrainedNetwork) 是學習特徵的例子，並非此板已可直接使用的承諾。替換 ORB 不只是換模型；descriptor、匹配、詞袋／閉環機制也要一起檢查。先建立 CPU baseline，再衡量 NPU offload 是否值得。

## 與朋友協作的具體驗收

請朋友交付可重播錄影＋原始時間戳、相機校正、pose log 和演算法設定，接本分支的 JSON API。不要只看點雲畫面判斷成功。

1. 靜止 30 秒：記錄位置／角度抖動與 drift，畫相機軌跡。
2. 各軸前後左右上下移動已知距離，再回到起點：確認符號、尺度、回程誤差。
3. 原地擺頭與轉身：區分真實相機繞頸部位移和演算法漂移；測 motion blur。
4. 遮鏡頭、看白牆、快速轉頭：必須回報 lost/relocalizing，前端不繼續移動。
5. 回到舊地方與重建 map：記錄重定位時間、loop closure 跳動，不要靜默改原點。
6. 量曝光到畫面更新的端到端延遲 p50/p95、追蹤更新率、CPU/RAM/溫度；API 接收 age 不是端到端延遲。

先以穩定 30 Hz 更新、p95 延遲 < 100 ms 作討論用目標（非實測與保證），實際容許誤差由展示需求決定。若板子跑不動，先減解析度／限制地圖／調整前端頻率，或重新討論追蹤運算分工，而不是假設有 NPU 就能解決。

下一步順序：確認 Logitech 型號與板端擷取支援 → 相機擷取和內參 → 固定 ArUco 公尺 baseline → 接朋友的 SLAM → 比較可靠性與成本 → 才做 NPU 部署。這版建立的是前後端共同契約，尚未交付無標記真機追蹤。
